{% for host in groups['webservers'] + groups['loadbalancer'] %}
   define host{
	   use             linux-server
	   host_name       {{ host }}
	   alias           {{ host | replace('web', 'Web-Server ') | upper }}
	   address         {{ hostvars[host].ansible_host }}
   }
{% endfor %}